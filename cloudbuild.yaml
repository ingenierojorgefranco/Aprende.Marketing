steps:
  - name: "gcr.io/cloud-builders/docker"
    args:
      - "build"
      - "-t"
      - "gcr.io/$PROJECT_ID/generatorladingdeployend:$SHORT_SHA"
      - "."

  - name: "gcr.io/cloud-builders/docker"
    args:
      - "push"
      - "gcr.io/$PROJECT_ID/generatorladingdeployend:$SHORT_SHA"

  - name: "gcr.io/google.com/cloudsdktool/cloud-sdk"
    entrypoint: gcloud
    args:
      - "run"
      - "deploy"
      - "generatorladingdeployend"
      - "--image"
      - "gcr.io/$PROJECT_ID/generatorladingdeployend:$SHORT_SHA"
      - "--region"
      - "europe-southwest1"
      - "--platform"
      - "managed"
      - "--allow-unauthenticated"

images:
  - "gcr.io/$PROJECT_ID/generatorladingdeployend:$SHORT_SHA"

options:
  logging: CLOUD_LOGGING_ONLY
